import 'package:flutter/material.dart';

class AppColor {
  // ── Primary palette ──
  static const Color primaryColor = Color(0xFF7C5CBF);
  static const Color primaryLight = Color(0xFFEDE7F6);
  static const Color primaryDark = Color(0xFF5A3D9E);

  // ── Accent / secondary ──
  static const Color accentBlue = Color(0xFF5B7FD6);
  static const Color accentPink = Color(0xFFE8A0B4);

  // ── Surface & background ──
  static const Color scaffoldBg = Color(0xFFF5F3F8);
  static const Color cardBg = Colors.white;
  static const Color backgroundColor = Colors.white;
  static const Color whiteColor = Colors.white;
  static const Color offWhiteColor = Color(0xFFF0EDEE);

  // ── Text ──
  static const Color textPrimary = Color(0xFF1E1E2D);
  static const Color textSecondary = Color(0xFF6E6E82);
  static const Color textHint = Color(0xFFA0A0B2);

  // ── Legacy aliases (keep existing references working) ──
  static const Color welcomeScreenInitialColor = accentBlue;
  static const Color headerTextColor = accentBlue;
  static const Color subTextColor = textSecondary;
  static const Color buttonColor = accentBlue;
  static const Color buttonTextColor = Colors.white;
  static const Color grayColor = textSecondary;
  static const Color blackColor = textPrimary;
  static const Color mainScreenButtons = primaryColor;
  static const Color myCourseScreenButtons = accentPink;
  static const Color savedIconColor = accentBlue;

  // ── Semantic ──
  static const Color greenColor = Color(0xFF34C759);
  static const Color starColor = Color(0xFFFFC107);
  static const Color priceColor = Color(0xFF2E7D32);
  static const Color errorColor = Color(0xFFE53935);
  static const Color logOutColor = Color(0xFFFF6F47);

  // ── Profile menu ──
  static const Color settingColor = Color(0xFFEBDBDE);
  static const Color supportColor = Color(0xFFC8BAD3);
  static const Color privacyColor = Color(0xFFB1D5CB);
  static const Color contactUsColor = accentBlue;

  // ── Basic ──
  static const Color white = Colors.white;
  static const Color red = Colors.red;
  static const Color black = Colors.black;
  static const Color gray = Colors.grey;
}
